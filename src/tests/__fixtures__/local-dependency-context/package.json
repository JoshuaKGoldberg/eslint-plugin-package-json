{
	"name": "local-dependency-context",
	"version": "1.0.0",
	"description": "Fixture used only to provide a nested package.json path context for valid-local-dependency tests.",
	"license": "ISC",
	"author": "",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"debug": "^4.1.0",
		"mocha": "^11.0.0"
	}
}
